# FastAPI 和 Web 服务器
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP 客户端（包含 brotli 和 zstandard 支持以处理压缩响应）
httpx[http2,brotli,zstd]>=0.25.0
brotli>=1.0.9
zstandard>=0.21.0

# JWT 处理
PyJWT>=2.8.0

# 结构化日志
structlog>=23.2.0

# 性能优化库
fastuuid>=0.7.0
orjson>=3.9.0

# 时间处理
python-dateutil>=2.8.0

# URL处理
furl>=2.1.0

# 其他工具库
browserforge>=1.0.0
python-multipart>=0.0.6

